# Copy this file to spark_credentials.env on the robot and fill in the values.
# Keep spark_credentials.env private because it contains API credentials.
export SPARK_API_KEY="your_key_here"
export SPARK_API_SECRET="your_secret_here"
